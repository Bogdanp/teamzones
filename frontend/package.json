{
  "name": "teamzones",
  "version": "1.0.0",
  "description": "the teamzones.io frontend",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browserify": "browserify"
  },
  "private": true,
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.0",
    "mjml": "^2.2.0",
    "node-sass": "^3.6.0"
  },
  "dependencies": {
    "moment-timezone": "^0.5.3",
    "whatwg-fetch": "^1.0.0"
  },
  "browserify": {
    "transform": [["babelify", { "presets": ["es2015"] }]]
  }
}
